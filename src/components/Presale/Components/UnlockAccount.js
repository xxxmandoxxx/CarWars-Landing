import React from 'react';

const unlockAccount = (props) => {
    return(
        <div>
            Your MetaMask account is locked. Please unlock your account to continue to the pre-sale.
        </div>
    )
};

export default unlockAccount;